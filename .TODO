☐ Inside `pakfile.rs`, move from storing the data as a `Vec<u8>` to a `Vec<InternalFile>`
☐ Most of `pakfile` should be moved to `fs`
☐ Add errors to more sub modeules
☐ Make a variant of `add_file` that does not read the entire file into memory